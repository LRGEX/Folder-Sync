[package]
name = "folder_sync"
version = "0.7.0"
edition = "2021"

[dependencies]
slint = "1.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
winreg = "0.55"
rfd = "0.15"

[profile.release]
lto = true
codegen-units = 1
strip = true


[build-dependencies]
winres = "0.1"
